import styled, { css } from 'styled-components';

export const theme = {
  colors: {
    white: '#ffffff',
  },
};

export const flexCenter = css`
  display: flex;
  justify-content: center;
  align-items: center;
`;
